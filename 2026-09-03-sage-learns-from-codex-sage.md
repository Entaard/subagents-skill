# Proposal: what sage-claude should take from Codex Sage

Date: 2026-09-03, revised 2026-09-04 (twice). Subject: `2026-08-31-sage-codex-proposal.md`, the `sage/` directory (Sage for Codex, Phase 0 and Phase 1), and the six root-level documents that describe and measure it. Target: `sage-claude/` in this repository.

The first 2026-09-04 revision answered four questions the first draft did not ask. Can the self-improvement loop be simpler? Why is Codex Sage's text so much smaller, and what does that teach? Would JSON serve better than Markdown? Does the successor-orchestrator handover still earn its place? The second revision came from a two-angle review of that text. One reviewer audited every figure and pointer against the corpus. The other audited the design for consistency, severity and work order. Three things changed. First, one premise fell: auto-compaction has fired on this machine, fifteen times inside one sage run, and no persistent record kept it. B2 is re-derived from that. Second, every item that is a rename, a one-word tag, a lint check for a condition never observed, or a grammar token with no recorded demand is cut. Four items survive. Third, the order of work now runs instruments first and deletions inside the router rewrite.

## Verdict in one paragraph

Codex Sage is a real implementation, not a paper design. Its Python library and its 44 tests run and pass on this machine once one portability defect is patched. Most of its policy is a port of sage-claude and brings nothing back. Its two large new mechanisms, the JSON run record with a Markdown projection and the receipt-bound installer, solve problems sage-claude has already solved another way, at a cost that no measurement justifies. What Codex Sage does show is shape: a router `SKILL.md` of 804 words, policy files with no dates and no anecdotes, and a memory boundary stated as one three-row table. The measurements add four facts. A sage-claude run loads about 41,700 words against Codex Sage's 9,716. At least sixteen sage-claude rules stand in two or more homes. The successor handover fired at least fourteen times, spawned a successor five times, and since 2026-08-27 has never spawned one. And the sage run `a58bd85c` on 2026-09-03 ran its parent on `claude-opus-5` in a 200k window, the user's model choice for that session, auto-compacted fifteen times, closed with its deliverable, and left no persistent record of it, because `sage-watch.sh` assumed a 1M window and the journal has no field for it. So four items survive. B1 makes `SKILL.md` a router and cuts the run's reading load. B2 retires the sensor-triggered successor as an experiment with a measured gate, and makes compaction the designed recovery, which the record shows it already is. A2 measures the model each actor ran on, parent included. The window comes from the compaction record, not from the model id, because the id is the same string in both windows. A5 adds a credential-shape check to the lint. Everything else from the first two drafts is cut as minor or rejected, and the reasons are kept below.

## Disposition

The four kept items rank by the size of their effect on every run. The order of work in the last section differs from this rank, because it follows dependencies. A2 lands first there, because B2's gate cannot be computed without it.

| Rank | Item | Why it sits here |
| --- | --- | --- |
| 1 | B1. Router `SKILL.md` and the reading-load cut | Touches every run and every reader. Measured: about 41,700 run-loaded words, at least 16 duplicate homes, 3,088 words of `memory.md` a run never uses. The user's stated preference. Carries the memory boundary table (formerly B3) and the deletion half of B2. |
| 2 | B2. Retire the successor handover, as an experiment | The largest single removal: about 3,500 words, an agent file, a recorded failure class. Measured: at least 9 of 14 firings were misfires, every one since 2026-08-27. The recovery path it relies on, native compaction over the ledger, has now been observed 15 times in one run. Its cost has not. So B2 carries a gate and a reopen condition. |
| 3 | A2. Measure the effective model from the transcript | B2's gate cannot be priced without the parent's model, and every unit's model cell is otherwise an assertion the harness can silently falsify. Script change only. |
| 4 | A5. Secret-shape lint | Script only. The leak path into a user's git history is measured, and the consequence class is credentials. |

Cut as minor, with the reason kept in "Items cut as minor": A1, A3, A6, A7, A8, A9, A10, the spend-provenance word, and B3 steps 3 to 5. Absorbed: A4 into B2, B3 steps 1 and 2 into B1, the `sha256sum` adaptation into B2 step 1, the host-leak grep into A5. Rejected, no change: B4 and R1 to R9. Unscheduled by design: A11.

## How this study was done, and what it found about the corpus

Six read-only scouts enumerated the corpus on 2026-09-03 into one line per mechanism with a file:line pointer. The parent ran Codex Sage's own checks and read the primary files that each verdict below depends on.

On 2026-09-04 four more scouts ran, each on the cheapest model that could do its job. A handover-evidence scout read all 5 handoff notes, the 21 ledgers under `.claude/plans/`, the installed memory archive and six lesson KIs. A duplication scout measured `sage-claude/SKILL.md` section by section and listed every rule with two or more homes. A router scout mapped how Codex Sage's `SKILL.md` points at its references. A web scout fetched primary sources on auto-compaction, the 1M window, the "20 to 30%" heuristic and cache-read pricing. Then two reviewers on Fable 5.1 checked the revised text from two angles, evidence and design. The evidence reviewer re-ran every count and resolved every pointer; it found the compaction record described under B2. The design reviewer found the stale text that the first revision left behind and the order-of-work defects. The scout and reviewer reports live in this session's scratchpad and die with it. Every figure this document relies on is quoted here with the command or the file:line that reproduces it. Where a figure came from a dead scout report and could not be reproduced, it says so.

Three facts about the corpus shape every verdict that follows.

**The code works, and one line stops it from running here.** `python3 sage/scripts/check-phase0.py --self-test` passes. `python3 sage/scripts/check-phase1.py` fails as shipped: 40 of 44 tests raise `FileNotFoundError` because `sage/tests/test_phase1.py` hardcodes `dir="/private/tmp"`, a macOS-only path, in 40 places (first at line 141). With that string replaced by `/tmp` in a scratch clone, all 44 tests pass in 38 seconds and the Phase 1 gate reports "implementation and frozen-gate checks passed". The repository also commits `cpython-311` bytecode from the author's machine. The README's instruction to run the complete Phase 1 matrix is therefore true only on the author's platform. This is not a reason to distrust the design. It is a reason to treat every "tested" claim as tested on one machine.

**No Codex Sage design has outcome evidence.** The Phase 1 paired pilot that was meant to show the policy's value never started (`sage/README.md`, "Phase 1 pilot gate"; `sage/evaluation/phase-1/STATUS.md`). The Managed mode, the runtime protocol, the capability negotiation and the resource coordinator exist as schemas and prose only. `sage/knowledge/index.json` holds zero promoted records, so the promotion workflow has never landed a real record. Every adoption below therefore has to justify itself on sage-claude's own measurements. Where it cannot, it is rejected or cut, however clean the Codex design reads. That rule is applied to the first draft's own items in "Items cut as minor".

**The effort that produced it was expensive.** `codex-massive-usage.md` lines 84 to 90 attribute 105.3M tokens to the proposal session group, 86.5M to the Phase 1 implementation, 98.1M to the later `$sage` instruction work, and 37.5M to the implementation review. The usage investigation (`codex-vs-claude-sage-usage-investigation.md`) traces the spend to a root model on maximum reasoning, thousands of tool cycles, repeated compaction and fan-out. None of that spend bought a single measured run of the skill itself. A design that consumed this much before its first evaluation is not evidence of value. It is evidence of scope.

## The four items kept

Each item states the current design first, then the evidence, then the change. Word counts are `wc -w` on the committed files at `bef7e35`.

### B1. Make `SKILL.md` a router, and cut what a run has to read

**What "15 times less" measures.** Codex Sage's `SKILL.md` is 804 words. sage-claude's is 12,526. That is the ×15 (15.6×), and it compares one file with one file. A run does not load one file. sage-claude's `SKILL.md` tells a run to read four reference files and two script headers: `topologies.md` at Step 1 (`SKILL.md:90`), `harness.md`, `memory.md` and `dispatch.md` at Step 2 (`:96`, `:104`), the `sage-watch.sh` header (`:173`) and the `sage-lint.sh` header (`:167`). The sum is about 41,700 words: `SKILL.md` 12,526; `references/topologies.md` 2,647; `references/harness.md` 8,542; `references/dispatch.md` 4,247; `references/memory.md` 4,185; the `bin/sage-watch.sh` header 2,257; the `bin/sage-lint.sh` header 7,374. Codex Sage's `SKILL.md` names eight policy files plus `codex.md` and `guarantees.md`. That sum is 9,716 words. The honest ratio is 4.3×, not 15×. The two promote skills compare at 8,357 words against 2,474, or 3.4×. Codex Sage also has no script headers to read, because its scripts are a Python library behind a CLI. Part of its text moved into code. That part is R1 and R5, and this proposal does not bring it back as prose.

**Where the 32,000 words come from.** Four sources.

1. **Duplicate homes.** At least sixteen rules stand at full strength in two or more places. The count of sixteen comes from a scout report that died with its session; every duplicate named here was re-verified on 2026-09-04, so sixteen is a lower bound by inspection, not a reproduced total. The alt-lane rule "no `model` parameter" appears four times in `SKILL.md` alone (`SKILL.md:132`, `:153`, `:201`, `:353`), twice in `references/harness.md` (`:141`, `:197`) and once in `references/dispatch.md:20`. The guard script's own header says "`../SKILL.md` states the rule three times" (`bin/sage-alt-guard.sh:21`). The duty to bring the ledger current appears on eight lines of `SKILL.md` (`grep -n -i 'bring-current\|ledger current' sage-claude/SKILL.md`). The parent-kept writer's `clean-code` load rule is a near-identical paragraph twice in the same file (`SKILL.md:77` and `:165`). `SKILL.md:173` says of the watchdog header "read it there; nothing below restates it". `SKILL.md:175` then restates the whole directory-resolution procedure, anecdote included. The 30% threshold has six homes. The `git check-ignore` four-branch rule has three. Two rules are single-homed and show the pattern to copy: "grep the claim before you brief it" (`SKILL.md:129`) and the `superseded → D<n>` tag (`references/dispatch.md:162`).
2. **Text a run never uses.** `references/memory.md` is 4,185 words. 3,088 of them serve `/sage-promote` or a maintainer: the shape, the KI field contract, the structural invariants, the compression floor, the promotion section. The run-facing sections (the preamble, the journal grammar, the Step 2 read, the Step 6 append, the hint) sum to 1,081. `references/harness.md` carries about 1,050 words of dated derivation that no run decision reads: the dedup percentiles, the sampling cost, the Mythos study, the occupancy arithmetic. The `bin/sage-lint.sh` header a run is told to read at Step 4 is 7,374 words. Its `CORPUS MODE` section, about 2,050 words, documents a mode a run never invokes.
3. **Anecdote and rationale.** Steps 3, 4 and 5 hold 5,963 words, 48% of `SKILL.md`. The scout's read puts about 30% of those words in anecdote and measurement and 20 to 25% in rationale. Codex Sage's policy files carry zero dates, zero "one run" anecdotes and zero calibration tags. sage-claude's compression floor (`references/memory.md`, `## The compression floor`) protects the recognising anecdote on purpose, so this share cannot reach zero. It can go down. `/sage-promote`'s stage two already names the compressible form: "the third subordinate tail", the clause after the rule and after its one justification (`claude-skills/sage-promote/SKILL.md:216`). It also names two cuts that do not work. Cutting anecdotes fails because half of them are recognisers. Converting prose to tables measured at −11%.
4. **The handover apparatus.** About 3,500 words: `SKILL.md` `## Handover` (1,623), the handoff-note template (451), the harness paragraph on occupancy (382) and the `orchestrator` agent file (1,072). B2 owns that.

**What the router shape buys, honestly.** Moving a paragraph from `SKILL.md` into a reference file saves nothing by itself. A reference the run reads lands in the same window. Three things do pay. First, a run reads a step's file at that step. Step 5 and Step 6 text is then absent from the window during Steps 1 to 4, and a run that stops on a rail never loads it. Second, the one-home rule becomes mechanical. A router `SKILL.md` carries no rule at full strength, so every rule has exactly one file to live in, and the duplicates above have nowhere to grow back. Third, a person can read the spine in two minutes, and a person is the reader the user named. The saving comes from the cuts the router makes possible, not from the move. Codex Sage keeps its generated copies in sync with a hashed manifest (`sage/scripts/generate-skill-bundle.py --check`). sage-claude already has the equivalent in `/sage-promote`'s `diff -rq` between repo and installed tree, so nothing new is needed there.

**Verdict: adopt.** The design:

1. `SKILL.md` becomes a router of at most 2,500 words. Its contents, each with a word budget:
   - frontmatter, the job sentence and the three invocation forms: 200
   - the four axioms: 150
   - the Defaults table: 280
   - the six steps, at most 150 words each, each ending with the file or files to read at that step: 900
   - the Rails table with its NEVER clause: 250 (the rest of today's 643 moves to `references/execute.md`)
   - the stop rule, one paragraph: 100
   - B2's `## Compaction and resume`: 200
   - the references list, one line per file: 150

   That sums to 2,230. The Defaults table stays here because it is the one block the user edits. `## Compaction and resume` stays here because the hook B2 step 2 installs says "re-read `SKILL.md`", and a compaction can land during any step.
2. Step bodies move to one file per step, mirroring Codex Sage's eight policy files:
   - `references/decompose.md`: Step 1. It also becomes the home of the one-writer-per-tree rule, because Step 1 decides it.
   - `references/dispatch.md`: Steps 2 and 3, which it already half-owns.
   - `references/execute.md`: Step 4, the failure ladder, the watchdog hosting, the lint cadence.
   - `references/verify.md`: Step 5.
   - `references/record.md`: Step 6, the ledger shapes and the run record.

   `topologies.md` and `authoring.md` stay as they are. Step 2 names three files: `dispatch.md`, the harness run sheet and the run-facing `memory.md`. All three are inside the run-loaded total below.
3. `references/harness.md` splits in two. The run sheet keeps the tier table, spawn mechanics, the window, the alt-lane rules, about 3,000 words. `references/harness-measurements.md` takes every dated figure, percentile and derivation. `/sage-promote` stage three and a maintainer read it; a run never does. The run sheet cites the measurements file wherever a figure has a date.
4. `references/memory.md` keeps the run's duties only, about 900 words, and opens with the memory boundary as one table. This is the former B3. Codex Sage states the boundary as a three-row table (`sage/skills/sage/references/policy/memory.md:9`); sage-claude's v3 design already obeys it (`references/memory.md:7`: a run's only write is an append of plain lines to the journal) but states it across 4,185 words. The table, in Codex Sage's form with sage-claude's names: row one, the corpus (`SKILL.md`, `references/`, `memory/shared/`, `memory/local/`), read at Step 2, never written by a run, written by `/sage-promote`. Row two, the run's own ledger, read and written by the run, read by `/sage report` and `/sage resume`, never read by a later run. Row three, the journal, appended by the run at Step 6, drained by `/sage-promote`, read by a later run for its `run` lines only. One sentence under the table names that last read as the one exception and its reason: a `run` line is an actual, not a lesson, and `price-off-a-same-shape-row` (calibration: established) says the actual beats the band. The read becomes `awk '$2=="run"' memory/journal.md | tail -n 3`, so that the read matches the row and does not match `obs` payloads that contain the word. The alternative, in which `/sage-promote` copies the newest `run` lines into a KI body and the run reads no log, is rejected because pricing data would lag one promote pass, eight to twelve runs. After the table, the file keeps the Step 2 read, the Step 6 append, the hint and the journal grammar. The KI field contract, the structural invariants and the compression floor move to `claude-skills/sage-promote/references/memory-contract.md`, because that skill is their only reader.
5. Each script header gains a run block of at most 300 words at the top, closed by a marker line. The step file tells the run to read up to the marker: `sed -n '1,/^# END RUN BLOCK/p' bin/sage-lint.sh`. The rest of the header stays for maintainers, unchanged.
6. The cut inside each step file has two passes. First, remove every duplicate the report lists: the owning file keeps the rule, and every other mention becomes a pointer or nothing. Second, apply stage two's third-subordinate-tail rule paragraph by paragraph, and keep every floor item. The `--corpus` lint's `cortex-budget` check today parses one row, `Cortex word budget | <N> words` (`bin/sage-lint.sh:500`). It gains a second row and a defined file set: `SKILL.md` at most 2,500 words, and the run-loaded total at most 20,000. The run-loaded total is the router, plus every file a step names, plus every script run block up to its marker. A file that only the references list names, `authoring.md` or the `sage-alt-guard.sh` header, is outside the total, and the lint header says so. Both figures are targets. The second one is what measures the design.
7. Verification, per file. `references/topologies.md` #12's stop arm runs on the pre-cut corpus for any paragraph whose removal the author doubts. `/sage-promote`'s one-home grep runs over the whole corpus after the batch. `bin/sage-lint.sh --corpus` runs last. Then `install.sh` re-installs, and `/sage-promote`'s `diff -rq` between repo and `~/.claude/skills/sage` confirms the installed copy matches, because a run loads the installed copy and not the repository. Land it as a `/sage` run on the user's word. Each step file is one writer unit under a write lease. A `verifier` per file gets the brief "name a run decision the old text settled and the new text does not".

**The arithmetic behind the 20,000 target, labelled an estimate.** The router (2,500), `topologies.md` (2,647), the harness run sheet (3,000), the run-facing `memory.md` (900) and two 300-word run blocks sum to 9,647. That leaves about 10,350 words for the five step files. Today the step bodies hold about 8,750 words and `dispatch.md` 4,247, about 13,000 together. So the step files need a cut of about 20% after deduplication. Falsifier: three runs after the cut record a `miss` or a deviation that the removed text would have prevented, or the run-loaded total lands above 25,000 because the step files grew back.

### B2. Retire the successor handover as an experiment; let the ledger and compaction carry the run

**The two reasons for the 30% rung, re-examined.** The rung rests on two beliefs. The first is that a model's useful context is 20 to 30% of its window. On 2026-09-04 the web scout searched Anthropic's context-engineering post, the Claude Code docs, Chroma's Context Rot report, Lost in the Middle, NoLiMa and RULER. None states a 20 to 30% figure for any Claude model, and Anthropic publishes no numeric effective fraction. Those sources show that quality drops as *irrelevant* content grows. The remedy they name is a small set of high-signal tokens. Sage already does that with capped reports, artifact hand-off and scouts. Restarting the actor at 30% adds nothing to it. The second belief is that auto-compaction would lose the run. The next paragraph shows that belief has already been tested, by accident.

**Auto-compaction has fired here, and the ledger carried the run.** `references/harness.md:248` says every compaction on this machine is `trigger:"manual"` and no auto-compact has ever fired. That was true on 2026-08-18. It is not true now. `grep -rhoa '"trigger":"[a-z_]*"' ~/.claude/projects/ | sort | uniq -c` prints 19 `auto`, 5 `manual`, 1 `refusal`. Fifteen of the nineteen are in one transcript: the parent of sage run `a58bd85c` on 2026-09-03 (`~/.claude/projects/-mnt-c-Code-OrangeLogic-Testing-Tools/a58bd85c-4c10-4d73-a2dd-ad2a44327730.jsonl`, the run the journal records at `journal.md:29`). Every one of its 467 assistant records carries `"model":"claude-opus-5"`. Its fifteen `compact_boundary` records show `preTokens` between 167,051 and 177,779. So that parent ran in a 200k window and compacted at about 84 to 89% of it, fifteen times over four hours. The user reports choosing the 200k variant of the model for that session by mistake, so the window was a session setting, not a harness swap. One more `auto` is in a subagent of the same run, one in a subagent of run `0042d31e` on 2026-08-18 (`claude-sonnet-5`, `preTokens` 168,094), two in an unrelated repository. The run closed with its deliverable and a clean `run` line. The parent did notice: the transcript shows it re-read the ledger right after the second compaction, ran the lint on the ledger after five others, and once wrote `at post-compaction bring-current` into the ledger header, a line the next bring-current overwrote. Its by-hand occupancy arithmetic after three compactions divided by 1,006,380, so it believed the 1M window throughout. The journal and the run record have no field for a compaction, so the persistent record kept none of it. The `run` line says "parent occupancy 142,331 (14.1%) at close", which divides by the 1,006,380 default that `sage-watch.sh` assumes (`bin/sage-watch.sh:241`). Against the actual window that figure is 71%, and it is a post-compaction figure. The 30% rung, 301,914 tokens, could never have fired in that run. Three conclusions follow. The successor's premise, that a compaction loses the run, has fifteen counter-examples in which the ledger alone carried a parent to a good close. The design's sensor was blind for the whole run, because it assumed a window it had no way to check; step 3 below gives it one. And the "first compacted run" this document's earlier revision planned to observe has already happened and left only a transient record, which is the first datum for the gate below. Whether that run re-dispatched a running unit or lost a finding is unknown, because nobody looked. Its ledger lives in the other repository and is not reachable from this environment, so the check runs on the transcript: every `Agent` dispatch after each `compact_boundary`, compared with the units dispatched before it, and every finding row the parent's `Edit` inputs wrote before and after. The ledger is a second source if the user supplies it.

**What the record shows the successor did.** At least fourteen rungs fired on this machine between 2026-08-18 and 2026-09-03. Five spawned a successor: `0042d31e`, `61264f83`, `cfff08b5`, `683518a1`, and a `/teach` corpus run on 2026-08-26. At least nine finished in place as a logged deviation: the eight the misfire lesson enumerates (`~/.claude/skills/sage/memory/local/lesson-occ-30pct-fires-with-nothing-to-launch.md`, `count: 8`, one run firing twice) plus `d8000479` on 2026-08-28 (`.claude/plans/sage-ledger-d8000479.md:102`), which the lesson does not list. Every firing since 2026-08-27 was in place. Two of those fired in Step 5, not at the close: `d8000479` during a fix round and `42dd56d4` with two re-verdict steers in flight (`.claude/plans/sage-ledger-42dd56d4.md:79`). All five successors did substantive work. One carried 1,346.9k of a run's spend through generation 2 (`~/.claude/skills/sage/memory/archive/local-v2.md:145`). One implemented 27 fix items across a five-file lease (`.claude/plans/sage-ledger-683518a1-fix.md:37`). The `cfff08b5` successor ran the round-2 fixes and the close (`.claude/plans/sage-ledger-cfff08b5-9e86-4edd-be44-e9cdc06c481d.md:60`, `:99`). That is the honest half: the mechanism carried the long runs of the first nine days.

**Why the later runs stayed small is not settled.** The runs since 2026-08-27 close between 14% and 33% by the journal's `run` lines, though the 14.1% figure is `a58bd85c`'s and uses the wrong denominator. This is consistent with the briefing and report-size rules that landed in that period. It is also consistent with smaller tasks. No run since 2026-08-27 is as large as the smallest run that spawned a successor. The 2026-08-26 run spent 4,103.3k in total, 2,044.9k of it on the parent side over three generations (`~/.claude/skills/sage/memory/archive/local-v2.md:145`). `0042d31e` was projected at 740k to 770k. The later runs' fleets spent 61k to 560k. So the record cannot say whether a 16-unit run under today's rules would still cross the threshold with work left. That is the strongest argument a defender of the successor can make, and it is why B2 is an experiment with a gate rather than a conclusion.

**What it costs.** In the corpus: about 2,460 words in every run's window, a 1,072-word agent file in the successor's window, and the `Generation` and `role` header fields with their lint check. Also the `SAGE_OCC_ACK` re-host, a steering relay because the successor's `tools:` line grants no `SendMessage` (`claude-agents/orchestrator.md:4`; `SKILL.md:317`), and a one-writer split between ledger and note. And a chaining invariant that holds "in prose only" (`SKILL.md:326`). In the record: one successor widened its own write lease without returning the rail-3 event (`.claude/plans/sage-handoff-61264f83-e776-4c65-b248-185c2ea2d83b-20260818-180447.md:149`). One returned below its threshold with two checker units still in flight for the parent to harvest (`.claude/plans/sage-ledger-683518a1-fix.md:67`). Successors priced by occupancy understated their cost by 3.4× on `61264f83` (`lesson-price-successor-by-spend-not-occupancy`; that successor spent 867,921 against 258,586 of occupancy) and by 4.55× in generation 2 of the 2026-08-26 run (`archive/local-v2.md:145`). A review finding on `ff95b189` predicted that a compaction during supervision would spawn a duplicate successor, and the header fields were added to prevent it (`.claude/plans/sage-ledger-ff95b189-5174-4f7f-8371-de4f3e6b15e7.md:98`); no such race is recorded. The design asks the parent for one `obs` line on each successor report (`SKILL.md:322`); what those lines found is not summarised anywhere. And the supervisor's own occupancy keeps climbing. The `0042d31e` supervisor closed at 39.76% (`.claude/plans/sage-ledger-61264f83-e776-4c65-b248-185c2ea2d83b.md:90`). So the successor slows the parent's growth without bounding it.

**What the successor was for, and what does the same job.** A successor is a controlled rehydration: a fresh window that reads the ledger. Native compaction with a `SessionStart(compact)` hook is the same rehydration in the same window. The hook exists (`install.sh:1061`, `offer_compact_hook`); today it is offered, not installed, and its message says to re-read the ledger. The ledger header already re-teaches the occupancy duty from line 1. Supervisor mode already rides compaction (`SKILL.md:324`). And `a58bd85c` rode fifteen of them without the hook. The parent's extra exposure is the state written since its last bring-current point, and step 3's checkpoint rung exists to close that gap.

The harness lets the user set the compaction point. `references/harness.md:260` and `install.sh:1185` name two knobs, `/autocompact <size>` and `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`, and the harness note says the percentage form fires earlier only and applies to subagents too. The ranges and the denominator quoted in this document's earlier revision came from a web scout whose report is gone, so they are unverified. `references/harness.md:260` tells the reader to verify the names against current docs; the installer's TIP at `install.sh:1183-1190` does not, and gains that sentence. Neither knob is required by this design, and nothing in the corpus can read a configured value back, so the design does not depend on one. Compaction fires with or without them; a user who sets one knows the point in advance and can tell the sensor (step 3).

**The cost question, stated as arithmetic the record cannot yet answer.** Every parent turn re-reads the whole window at the cache-read rate: 0.1× the input price on most models, 0.025× on Fable 5.1 and Mythos 5.1. A successor resets the working actor from about 300k to about 60k. That saves (300k − 60k) × the rate per turn: 24k input-equivalent tokens on Opus, 6k on Fable. Its fixed cost is the boot read, about 60 to 70k of cache creation at 1.25×, or 75 to 88k input-equivalent. The note's write cost no longer counts, because step 1 makes the ledger carry the same state at every bring-current point in either design. No boot has been measured alone: the one figure the earlier revision used, `cfff08b5`'s successor at 65 to 70k, included round-2 fixes. On those estimates the successor breaks even inside three to four turns on Opus and thirteen to fifteen on Fable. Against that saving stand the supervisor's own turns at 300k and above, which the record shows continuing to 39.76%. Neither actor's turn count is recorded anywhere, and the journal's `spend` formula excludes `cache_read` by definition (`references/harness.md:231`). So the cost of high occupancy has never been measured on this machine, in either design. Step 5 below adds the instrument. Until it reports, cost is not a ground for either keeping or retiring the successor.

Codex Sage, for comparison, never spawns a successor. It writes `handoff.json` and hands over explicitly, and it records automatic detection as unavailable (`sage/skills/sage/references/codex.md:65`).

**Verdict: adopt the user's proposal as an experiment, in this form.** The user's instinct is that notes plus the ledger are enough for one parent. The record supports that for every run since 2026-08-27, including one that compacted fifteen times, and cannot yet speak for a run the size of the early ones. The design:

1. **The ledger absorbs the note.** Two sections join the ledger. `### Resume state` holds the write lease, the snapshot baseline with one `sha256sum` line per task-owned file, the current step, the next action, and the `agentId → description` map with the `subagents/` directory path. The `sha256sum` line is what Codex Sage's handoff carries per baseline file (`sage-handoff-v1.schema.json:41`); `git status` alone misses untracked and out-of-repo files. `### Open questions and discarded approaches` holds the note's `Open questions` and `Discarded` fields. Both are restamped at every bring-current point, which is already a duty. The handoff note file and its template go. `/sage resume [ledger-path]` reads the ledger and re-runs the `sha256sum` lines.
2. **Compaction is the designed recovery.** `install.sh` installs the `SessionStart(compact)` hook by default, where today it offers it. Today's installer also returns without writing when any `SessionStart(compact)` hook exists (`install.sh:1087-1089`), so every machine that accepted the earlier offer, this one included, would keep the old message that says only "re-read the run ledger". The installer therefore matches its own `sage-reanchor` marker and rewrites that hook's command in place, and leaves a marker-less user hook alone. The hook's message becomes: re-read `### Resume state`, then re-read `SKILL.md`, then the step file that `### Resume state` names; dispatch nothing until all three are read. After a compaction the parent re-hosts the watchdog from the ledger's `subagents/` path, unless `### Resume state` shows the checkpoint already re-hosted it. A filed Claude Code issue reports background task handles orphaned across the boundary. That is a report, not a documented behaviour, and the `a58bd85c` transcript is the first place to check it: its watchdog notifications after each `compact_boundary` are either present or absent. The `/autocompact` knob is mentioned, not required, and the installer says so.
3. **The sensor stays; the rung changes.** `sage-watch.sh` keeps reading occupancy, but it stops trusting a window it cannot check. `a58bd85c` shows why: a default of 1,006,380 against an actual 200k window made every percentage fiction and silenced the rung. The model id cannot fix that. `message.model` is `claude-opus-5` in that 200k transcript and in a 1M transcript on this machine alike, and the `[1m]` suffix the settings carry never reaches the assistant records. So the expected compaction point comes from evidence, in this order. First, the transcript's own `compact_boundary` records: once one exists, the largest `preTokens` is the measured trigger for this session, and the script prints `window=measured`. Second, a value the user states at Step 2 because they set `/autocompact` or chose a model variant, passed as `SAGE_COMPACT_AT`. Third, `SAGE_WINDOW` minus a reserve, printed as `window=assumed`. The reserve is measured at 22 to 33k on the 200k window (the trigger sat at 167 to 178k) and unknown on 1M, so the default is the stamped window minus 30k, labelled estimated; the record's largest 1M occupancy, 466,802, never reached it. The rung fires once occupancy crosses the expected point minus a margin of `max(5% of window, 30k)`. The floor exists because 5% of 200k is 10k, about one minute of the measured parent burn and less than one large tool result; it is an estimate until `occ-sum` per turn measures next-turn growth. The rung fires once, not on every sample: the script fires only when the latest record is the first at or above the threshold since the newest `compact_boundary`, which is stateless and needs no acknowledgement. Today's rung repeats every sample until `SAGE_OCC_ACK` silences it (`bin/sage-watch.sh:145`, `:500`), and step 4 deletes the ack. The rung's action is a checkpoint, not a handover. Bring the ledger current. Restamp `### Resume state`, including a `checkpoint: turn <n>` field so a compacted parent knows the checkpoint ran and so the `run` line has the turn index. Write one `### Decisions and deviations` row with A4's arithmetic: room remaining, work remaining, reserve. Surface it. That is the one useful thing a parent can do before a compaction, and it is what the last nine firings already did by hand as deviations.
4. **Delete** the `orchestrator` agent, supervisor mode, chaining, the `Generation` and `role` header fields and the lint `header-fields` check on them, `SAGE_OCC_ACK`, and `## Handover`. A `## Compaction and resume` section of about 200 words replaces it, in the router (B1 item 1). The deletion reaches every place that names five agent files or the orchestrator: `/sage-promote`'s one-home grep over `~/.claude/agents/{explorer,implementer,orchestrator,verifier,web-researcher}.md`, `bin/sage-lint.sh`'s corpus-figure scope (`:447`, `:723`), `uninstall.sh:59`, `references/harness.md:28` (the tier-table row) and `:55`, the ledger header template at `references/dispatch.md:96`, and the `sage-watch.sh` header's rung and ack lines. `install.sh:481` syncs agents without `--delete`, so removing `claude-agents/orchestrator.md` from the repository leaves the installed copy in `~/.claude/agents/` and auto-delegatable; `install.sh` gains one guarded removal of that file, with a backup, and `uninstall.sh`'s agent list drops to four.
5. **Record what the design has never measured.** The `run` line gains five fields. `model=<parent model>` from the parent transcript, because the cache-read rate depends on it; the rate table (0.1× for most models, 0.025× for Fable and Mythos) lives in the script's run block beside it. `compact=<n>` counts `compact_boundary` records, each with `preTokens`, `cumulativeDroppedTokens` and the trigger; those three are transcript fields. What the parent re-read afterwards is not a field and is inferred from the next `tool_use` records. `turns=<n>` counts the parent's deduplicated assistant records. `occ-sum=<tokens>` is the sum of occupancy over those turns, the cache-read cost the `spend` formula excludes. `saving-post-rung=<tokens>` is the sum, over the turns after the checkpoint rung fired, of (occupancy − 60k): the most a successor could have saved on this run, because it ignores the supervisor's own turns. `sage-watch.sh --status` reads the parent transcript and computes the first four in the one `jq` program it already runs over every assistant record (`bin/sage-watch.sh:470-494`). For the fifth it prints the turn at which occupancy first crossed its current threshold; the parent copies the turn from the `checkpoint:` field it wrote at fire time, because the threshold can differ between the two invocations. `--status` also prints `compact=<n>` per unit, and a non-zero unit count is a `### Decisions and deviations` row and a surfaced event, because a unit that compacted reported from a summary of its own work. `a58bd85c` and `0042d31e` each have one such unit already. The first five compactions recorded this way are the design's first measurement. `a58bd85c` supplies fifteen retroactively: the fields can be computed from its transcript today, before any corpus change lands, and that computation is the first task in the order of work.

**The gate and the reopen condition, stated now.** The experiment fails on any one of three observations. A post-compaction parent re-dispatched a unit the ledger showed as running. A post-compaction parent lost a finding the ledger should have held. Or `saving-post-rung`, priced at the cache-read rate of the recorded `model=`, exceeds the successor's fixed boot cost of about 75 to 88k input-equivalent tokens. That comparison favours the successor, because `saving-post-rung` ignores the supervisor's own turns, so a gate that fires on it is conservative. If any fires, the design to reach for is a plan-time phase split, not a sensor. At Step 2, a plan whose shape predicts the parent past the checkpoint rung names the phase boundary where a fresh owner takes the remainder. The sensor-triggered successor does not return, because the record shows it fires at the wrong moment.

**What this supersedes.** A4's response arithmetic survives as step 3's row. A4's decision to keep the 30% rung and its `SAGE_OCC_ACK` acknowledgement do not. R9's "no change" verdict on supervisor mode is superseded, because step 4 deletes supervisor mode. `references/harness.md:248`'s "no auto-compact has ever fired here" is false and moves, corrected, to the measurements file B1 creates.

### A2. Measure the effective model from the transcript instead of asserting it

**Codex Sage.** Placement records `requested_model` and `effective_model` separately, and `effective_model` may be `unknown` (`sage-artifact-v1.schema.json:353`; `sage/skills/sage/references/guarantees.md:14`). Codex cannot observe the effective model, so the design preserves the unknown instead of copying the request.

**sage-claude today.** The unit table's `model used` cell is filled by the parent from what it passed (`references/dispatch.md`, Unit table). `bin/sage-watch.sh --status` prints occupancy and spend per agent but no model (`grep -c -i model bin/sage-watch.sh` prints 0). The window is a constant, `SAGE_WINDOW` defaulting to 1,006,380 (`bin/sage-watch.sh:241`). The alt lane already reaches for measurement: `references/harness.md`, "The alt lane", settles family identity by grepping the transcript.

**Verdict: adopt, for units and for the parent.**

**Reason.** Claude Code writes `message.model` on every assistant record of every transcript, parent and subagent alike. B2's gate prices `saving-post-rung` at the cache-read rate, and the rate is a function of the model, so the gate needs this field. The model id does not carry the window: `a58bd85c`'s 200k parent and a 1M parent on this machine both record `claude-opus-5`, so the window is B2 step 3's problem and this item does not claim it. `references/harness.md`, "Models and effort", lists two paths that silently swap a requested model, the `CLAUDE_CODE_SUBAGENT_MODEL` override and the `availableModels` fallback, and says "either path turns every Model cell in the ledger into fiction". The transcript is the one place the fiction cannot reach. Codex Sage has the right column and no sensor. sage-claude has the sensor and no column.

**Design.**

1. `bin/sage-watch.sh --status` prints `model=<value>` per agent and for the parent, taken from the latest assistant record's `message.model`, or `model=unknown` when no assistant record exists yet. About ten lines of `jq`, beside the fields it already prints.
2. `sage-watch.sh` prints the window source beside the model, `measured`, `stated` or `assumed`, as B2 step 3 defines them. No model-to-window table: the corpus has none (`references/harness.md:90-92` is prose that calls Opus a 1M model, which is what the sensor believed on `a58bd85c`), and the id cannot supply one.
3. The unit table's `model used` cell records the measured value, with the word `measured`, at the bring-current point where the parent reads `--status`. A cell the parent fills from its own dispatch says `asserted`.
4. A unit's measured value whose family or tier differs from the Plan row is a `### Decisions and deviations` row, written when it is read, because it means a swap happened that the run did not order. The parent's model is recorded, not judged: the user may choose it per session.
5. The Step 6 run record's Agents line and the journal `run` line carry the measured models, which is what makes the audit surface true after the fact. B2 step 5's `model=` field is this item.

Cost: one script edit and two template words. A lineup claim about which model ran becomes a grep, not a belief. The `a58bd85c` transcript is the test case: the script must print `claude-opus-5` for the parent, `compact=15`, and `window=measured` at 177,779.

### A5. Add a secret-shape check to the ledger lint

**Codex Sage.** `sage/lib/facts.py:69-101` validates every fact appended to a run log: it rejects payloads over a size cap, requires a hash and locator for confidential facts, and rejects credential-shaped strings with the message "a current-run fact payload appears to contain restricted credential material" (`facts.py:101`). The skill's guarantees list "credential/payload rejection" as enforced (`guarantees.md:19`). `check-phase0.py` separately scans portable text for host-specific names (`host_leak_issues`).

**sage-claude today.** The ledger is free text. Evidence cells quote command output. `references/harness.md:262` measured that `.claude/plans/` is not gitignored in a fresh repository, so a ledger can enter a user's history with one `git add -A`; only this repository ignores it (`.gitignore:1`). Nothing scans it. No leak has been observed. The class of the consequence, credentials in a git history, is what puts this above the bar.

**Verdict: adopt, as a lint check.**

**Reason.** The write-time gate Codex Sage uses does not exist in a Markdown ledger, but the lint runs at every bring-current point, which is the same moment. The check is deterministic, costs a regex, and fails open on unknown formats, which is the lint's existing contract. The lint already blanks fenced blocks before it reads (`bin/sage-lint.sh:32-33`), so "outside fenced blocks" costs nothing.

**Design.** `sage-lint.sh` gains `secret-shape`: one line per match of a small fixed pattern set on the whole file, outside fenced blocks: `AKIA[0-9A-Z]{16}`, `-----BEGIN [A-Z ]*PRIVATE KEY-----`, `ghp_[A-Za-z0-9]{36}`, `xox[baprs]-[A-Za-z0-9-]{10,}`, `Bearer [A-Za-z0-9._-]{20,}`, `sk-[A-Za-z0-9]{20,}`. The header states the blind spot: it knows these six shapes and nothing else. A hit at Step 6 is a surfaced event, like any other lint line. The same check runs in `--corpus` mode over `memory/`, because knowledge items are committed to the repository and `sage/skills/sage-promote/references/workflow.md:15` names the leak that matters there: credentials, private paths, user identity. The `--corpus` pass also flags absolute paths, session ids and `k`-suffixed figures inside `memory/shared/`, which is Codex Sage's host-leak scan at the cost of one more grep; the memory records two promote passes that wrote machine-local content into `shared/` (`defect-this-machine-wrote-shared-bands-down`, now dropped with its subject).

## Items cut as minor

The bar, stated once: an item stays only if it changes what every run loads or does with a measured effect, closes a measured failure, or closes a credential-class risk. A rename, a one-word tag, a lint check for a condition no ledger shows, a grammar token with no recorded demand, or a table that restates existing text is cut. Each cut item keeps one paragraph here so that the comparison with Codex Sage is not lost. Line references point at the corpus at `bef7e35`.

**A1. Evidence class per criterion.** Codex Sage tags every criterion `machine`, `agent_observable` or `human_only` (`sage-artifact-v1.schema.json:90`) and the run record at `references/dispatch.md:189` asks to name "any machine-verifiable case that downgraded to judged" with nothing earlier to compare against. The argument is sound. No ledger records a downgrade the tag would have caught; 6 of 21 mention the word. A bracket in the Plan template plus a lint check for a label is a tag, not a mechanism. Cut. If the router rewrite wants the distinction, the three bracket words fit inside the Plan template at no cost, without the lint check or the Step 5 sentence.

**A3. Coordination state word.** An enum for the run record's coordination check (`sage-artifact-v1.schema.json:550`) so that `/sage-promote` can count how often the fan-out bought nothing. Argued, unmeasured, by its own label; no memory item records a run where the prose form blocked a count. Cut under the rule at "No Codex Sage design has outcome evidence".

**A6. Promotion bar split by evidence class.** One reproduction should graduate a deterministic fact where an empirical rule waits for three confirmations (`sage/skills/sage-promote/references/promotion-contract.md:17`; sage-claude's bar at `references/memory.md:53` and `claude-skills/sage-promote/SKILL.md:157`). The argument is sound and the number is arbitrary in both directions: the earlier revision admitted that three is kept "by inertia". A threshold change on argument alone, in a pass the user runs by hand. Cut. Codex Sage's number six is rejected for the reason the follow-up report gives itself: recalibrate "from outcomes not intuition" (`sage-codex-follow-up-report.md:154`).

**A7. Novelty disposition at minting.** Record which KIs a new KI was compared against and one of `novel`, `revises`, `overlaps` (`knowledge-record-v1.schema.json:96`). sage-claude's minting step already requires the comparison (`claude-skills/sage-promote/SKILL.md:120`); this would record its result. Three words in a hand-run pass. Cut.

**A8. Two lint cell checks.** An `accepted` finding with an empty evidence cell; a `reported` unit with an empty `actual tokens` cell. The shape exists (`triage-state`, `bin/sage-lint.sh:166`) and the columns are labelled (`references/dispatch.md:137`, `:171`). A positional scan of the 21 ledgers found no instance of either condition. A check for a condition never observed. Cut.

**A9. Enforced-versus-advisory table.** Codex Sage's sixteen-row guarantee table (`sage/skills/sage/references/guarantees.md:7-22`). sage-claude states the same facts in four homes (`SKILL.md:157`, `references/harness.md:201`, `references/dispatch.md:16`, the `sage-alt-guard.sh` header). A table restating four homes risks becoming a fifth, and no run has mistaken an advisory line for an enforced one. Cut. The harness run sheet B1 creates may carry it if it fits the 3,000-word budget after the cut, and not otherwise.

**A10. Missed-recogniser clause.** A `use` line token for a KI that was not loaded but whose rule applied (`sage/policy/memory.md:15`). Argued, unmeasured; the journal holds no instance. A grammar token with no recorded demand. Cut.

**The spend-provenance word.** Tag each `actual tokens` cell `probe`, `notif` or `projection` (`2026-08-31-sage-codex-proposal.md:728`). The journal already writes `probe-measured` and `transcript-measured` by hand, and the Rails already require naming projection or actual on a surfaced rail. One word. Cut.

**B3 step 3, delete the archive reconciliation walk.** All 72 files under `memory/local/` carry `reconciled:` (`for f in ~/.claude/skills/sage/memory/local/*.md; do grep -q '^reconciled:' "$f" || echo "$f"; done` prints nothing), so `claude-skills/sage-promote/SKILL.md:126-133` can never find work again. About 330 dead words in a hand-run pass, costing one grep per pass. Cut from this proposal. It is a one-line deletion for whoever next edits that file.

**B3 step 4, rename `band:` to `calibration:`.** The word `band` means a cost band KI and a rule's strength. The rename is real and it is a rename. It is also larger than the earlier revision said: `band` appears on 23 lines of `claude-skills/sage-promote/SKILL.md`, 18 of `references/memory.md`, 9 of `SKILL.md`, 6 of `bin/sage-index.sh` and 6 of `bin/sage-lint.sh`, mixed between the two meanings. Cut.

**B3 step 5, a `weigh` token and a falsifier against the strength band.** No `use` line in the journal or its archives records a weighing, and the grammar has no slot for one (`references/memory.md:88`). The proposal was to add the slot and file a gap KI whose falsifier fires after ten empty runs. It removes nothing until then. Filing a gap KI is one journal `obs` line at any run's Step 6, not a work item. Cut.

**A4, handover response arithmetic.** Absorbed into B2 step 3. The evidence behind it is kept: the misfire lesson at `count: 8` with the sharper predicate from its fifth occurrence, "nothing in flight and the deliverable durable", and the usage investigation's recommendation 1 (`codex-vs-claude-sage-usage-investigation.md:196-212`), which replaces a fixed fraction with `remaining = window - occupancy` against a required headroom and asks that every term record how it was measured. Its first-draft design, which kept the 30% rung and `SAGE_OCC_ACK`, is withdrawn.

**A11, a pre-registered paired evaluation.** Codex Sage's frozen two-arm pilot (`sage/evaluation/phase-1/`) never ran because Codex exposes no hard per-arm cap. sage-claude has the cap in rail 4 and two open gap KIs the method could close, `gap-unattended-vs-approved-plan-unmeasured` and B2's experiment. The method is sound. It is a design to keep, not work to start: five to ten paired tasks, arms "plan approved" against "unattended", scoring by a clean-context `verifier` against the rubric's six dimensions, pre-registration hashed into the ledger. Unscheduled. Run it only when a gap KI has been open long enough that its cost exceeds the pilot's.

## B4. JSON for guidance or state: rejected

Codex Sage's guidance is Markdown. Its JSON is elsewhere: schemas, fixtures, the run record, the knowledge index and the source manifest, each validated by a Python library. The question was whether agents act better on JSON rules. Nothing measured here says so. Claude Code's skill format is Markdown by contract. The ledger lint already parses Markdown tables. R1 rejected a JSON ledger for a reason that still holds: the measured failures were rows not written, and a format cannot make a model write a row. Keep Markdown everywhere. The one JSON mechanism worth naming is a source manifest with per-file hashes that `generate-skill-bundle.py --check` verifies. `install.sh` and `/sage-promote`'s `diff -rq` between repo and installed tree already do that job.

## The usage investigation's other recommendations, one line each

Recommendation 1 is A4, absorbed into B2. Recommendations 4 and 6 are R7, 5 is R8, 8 is R9. The rest:

- **Rec 3, separate occupancy, spend and progress rails (`:219`).** Already separate in sage-claude: the occupancy rung, rail 4, and the failure ladder's same-signature stop. No change.
- **Rec 7, bound worker occupancy and return size (`:258`).** sage-claude bounds reports at 1–2k tokens. No change.
- **Rec 9, reuse a verifier thread only for a tight re-check (`:274`).** This is sage-claude's own rule, cited back from its memory. No change.
- **Rec 10, replace the blanket 4× with named cost profiles (`:280`).** Touches the open KI `gap-4x-budget-multiplier-may-be-consumed`, which records that every past ceiling raise was spent up to the new ceiling. A profile is a differently named ceiling and does not answer a consumption pattern. Rejected; the KI stays open, and the honest estimate-versus-actual journal line remains the instrument.
- **Rec 11, record quota before and after each phase (`:290`).** sage-claude records estimate against actual per run and per unit. Per-phase adds a duty with no reader. No change.
- **Rec 12, the matched handover experiment (`:294`).** Its subject is B2's experiment. The instrument is B2 step 5.

## Items rejected, with reasons

### R1. JSON run record as authority, Markdown as a hash-bound projection

Codex Sage makes `run.json` plus an append-only `facts.jsonl` the authority and renders `run.md` from it (`sage/scripts/sage-light.py`: `start` 353, `commit-state` 486, `append-fact` 535, `render` 559; `sage/lib/artifacts.py`). Schema and semantic validation reject illegal states before they land.

Rejected for sage-claude. The ledger's measured failures were compliance failures: ledgers written once and post-hoc, dispatches never entered, findings parked in illegal states (`SKILL.md`, Step 4, the lint paragraph). A JSON authority moves the same duty from "write the row" to "call `commit-state`". A model that skips the row skips the call. The one thing JSON buys, semantic validation, is what A5 takes into the text lint at the cost of a few awk lines, and what A1, A3 and A8 would have taken for conditions no ledger shows. What it would cost: every state change becomes a CLI round trip through the parent's context, the working record stops being readable by a person mid-run, and a compaction-time reader loses the header comment that today re-teaches the occupancy duty from line 1. No measurement shows the JSON path produces better-kept records, because no Codex Sage run has been kept.

### R2. Immutable plan revisions

Codex Sage forbids editing an admitted plan; a change is `PlanRevision N+1` with `prior_units` dispositions (`sage-artifact-v1.schema.json:252`; `sage/policy/contracts.md:9`).

Rejected. `references/dispatch.md`, Decisions and deviations, records the audit that produced the current rule: amendments were leaving the rows they amended untouched. The fix chosen was the `superseded → D<n>` tag on the amended row plus a Decisions row, with the sentence "a fifth representation of the plan cannot fix non-compliance with the four that already exist". A revision series is that fifth representation. No new evidence has arrived since the tag rule was written, and the lint already strips the tag when it compares Plan and Unit id sets.

### R3. A run may write facts only, never observations

Codex Sage forbids the active run from recording any lesson (`sage/policy/memory.md:10-13`; `test_phase1.py:287` rejects `lesson.extracted`). Only a later promotion pass may mine the closed log.

Rejected. sage-claude's `obs` lines are written at Step 6 by the parent that holds the run's whole context, with a falsifier attached, and they flip no status. The journal shows what they are worth: run `5f8bfa2d` recorded that six of nine review findings were defects the parent's own fixes introduced, a lesson no fact log without the parent's reasoning would carry. Codex Sage's promotion pass would have to reconstruct that from facts, and it has never run on a real closed log to show it can. The point where both designs agree, that the run consolidates nothing and promotes nothing, is already sage-claude's v3 rule.

The reading half of the same Codex rule is rejected too. `sage/policy/memory.md:13` forbids an active run from scanning "raw prior ledgers, journal tails, closed logs, archives for same-shape precedent". sage-claude's Step 2 requires exactly that read: the journal tail since the last `mark` is where same-shape run lines come from, and the live shared rule `price-off-a-same-shape-row` (calibration: established) says a same-shape logged row beats band arithmetic. Adopting the Codex clause would retire that rule with no measurement against it. The Codex rule protects a Codex weakness, a root that re-reads whole logs into a 258k window; sage-claude reads a few tail lines and records on the `use` line whether each read helped. B1 item 4 keeps that read as the one named exception in the boundary table.

One adjacent field gets a verdict here rather than nowhere: Codex Sage's promotion candidate carries `expected_benefit`, `expected_cost` and a `net_assessment` (`promotion-contract.md:17`; `knowledge-record-v1.schema.json:85`). Rejected. sage-promote already gates every candidate with a falsifier and a refuting degradation gate; a self-declared net assessment is a field the author fills in its own favour and no later stage reads.

### R4. Capability negotiation, runtime protocol v0, `sage-core`, Managed mode

The proposal's architecture sections 2 to 5 and the `sage/runtime/protocol-v0/` schemas define principals, capability snapshots, observation envelopes, leases with fencing tokens and crash-safe dispatch.

Rejected for now. Claude Code exposes no scheduler, lease or approval surface for a core to own, and the proposal's own Phase 2 gate, which was to decide whether Managed mode is needed, never ran. The usage investigation's numbers show what building the design's first two phases cost without a single evaluated run. sage-claude's design already names its enforcement gaps honestly (`references/harness.md`, "Cautions"). Reopen only if a measured run shows a failure that a saved agent file, a hook or a script cannot close.

### R5. The receipt-bound installer

`sage/lib/lifecycle.py` (2,517 lines) and 13 tests in `LifecycleTests` implement an ownership receipt with journal phases, resource identities, backups and recovery from interruption at every phase.

Rejected as a port. `install.sh` already keeps timestamped backups outside the auto-discovered directories and writes manifests that `uninstall.sh` reads to decide what it owns (`install.sh:121-142`; `uninstall.sh:197-216`). The installer defects sage-claude has measured, a `.bak` clobbered on the second install and a guard testing `[ -f ]` on a directory, were found by the installer-execution verifier in Step 5 and fixed. No incident of an interrupted install or an unrecoverable uninstall exists locally. The receipt design solves a problem that has not occurred, in a language the installer does not use. Keep the execution-verifier check. Revisit if an uninstall ever removes something it did not own. One installer gap this review did find is B2 step 4's: the agent sync has no `--delete`, so a removed agent file stays installed.

### R6. Placement by requirement vector instead of tier

Codex Sage records `corpus_size, ambiguity, reasoning_steps, tool_needs, latency_preference, cost_ceiling, independence_required, verification_criticality` per unit and resolves them through a host profile (`sage-artifact-v1.schema.json:171`; `sage/policy/delegation.md:72`).

Rejected. sage-claude's tier table is resolved against the live session at Step 2 and already crosses tier with step count (`references/harness.md`, "Models and effort"). The requirement vector has no calibration behind it, and it adds eight fields per unit that the parent fills by judgment, which is the same judgment the tier column records in one word.

### R7. Lower fan-out and the model-placement profile from the usage investigation

Recommendations 4 and 6 (`codex-vs-claude-sage-usage-investigation.md:231, 254`) cap fan-out at two concurrent children and three total, and move the root off the top model.

Rejected as Codex-specific. The spend pathology they answer is a root on maximum reasoning at twenty times the cheap model's cached rate, across thousands of cycles. sage-claude runs the parent on the apex model by a stated design decision (`references/harness.md`, "The parent is apex's real home"), caps at four, and prices every run against its own estimate. The evidence does not transfer. One mechanism in the recommendation is already in place: no child inherits the parent's model silently, because every dispatch names its model. A2 now measures whether that held.

### R8. Cycle and compaction rails

Recommendation 5 adds rails at 25 root turns and 50 tool calls, and on compaction count.

Rejected. The numbers are unmeasured, and sage-claude's rails are multiples of the run's own estimate with floors (`SKILL.md`, Rails). The local analogue of runaway parent cost, run `87b40637` where the parent came in 60% over while the fleet came in under, was priced wrong, not counted wrong. B2 step 5 records turns and compactions without making either a rail.

### R9. Root and successor overlap

Recommendation 8 (`:270`) wants the old root to supervise only unresolved handles after handover and to stop substantive work, and to record the overlap cost.

Superseded. On 2026-09-03 the verdict was "no change", because sage-claude's supervisor mode already limited the parent to steers, rails, the watchdog and one spot check. B2 step 4 deletes supervisor mode, so the comparison has no subject.

### Not adoptable because they are ports

Explicit-only activation, the six-step spine, the four axioms, the safe-and-worth tests, the topology catalogue, the failure ladder, the four rails, the snapshot protocol, one writer per tree, disjoint review lenses, the blind acceptance suite, the completeness critic, and the clean-code and diff-review policies in `sage/policy/implementation.md` and `software-review.md` are ports of sage-claude text. `sage/docs/phase1/equivalence.md` says so. Nothing comes back from them.

## Two things that look strong and are not

**The 63 invalid fixtures and 44 tests.** They prove that the Python library rejects the states its own schema forbids. They prove nothing about whether a Codex root model writes those states in a real run, because none has been recorded. The lint in sage-claude states the same limit about itself: it "reads legality, never liveness". Fixture count is not evidence of kept records.

**The sixteen-row guarantee table.** It reads as rigour. But its content for Codex Light is mostly "advisory", "policy bookkeeping", "unknown unless supported". It is an honest inventory of what Codex Sage cannot enforce. It is not a list of things sage-claude lacks, and A9 is cut for that reason.

**The "15 times less text".** True for one file against one file. The router shape it points at is worth taking (B1). But a run loads its references. On that count the ratio is 4.3×, and 3.4× for the promote skills. About a third of the gap is text that Codex Sage moved into Python. R1 and R5 reject bringing that text back as prose, so it is not a saving available to sage-claude.

## What this proposal does not settle

- B2's checkpoint rung has two estimates in it before the first compaction of a session. The 30k reserve is measured on a 200k window only; the 1M trigger has never been observed, so on a 1M session the rung may fire late or not at all, and the exposure is then the bring-current cadence. The 30k margin floor is a guess. Both are labelled in the script's run block, and the first `compact_boundary` replaces them with a measurement for that session.
- After the checkpoint rung fires, nothing else fires. The parent's exposure to a compaction is the state since its last bring-current point, and that bound rests on the bring-current cadence, which `SKILL.md:169` records as bookkeeping prose that runs skipped. A gate failure caused by a skipped bring-current looks the same as one caused by the design. The checkpoint row's turn index and the `compact=` field are what tell them apart.
- A subagent that compacted reports from a summary of its own work. B2 step 5 makes that visible after the fact through `--status`; nothing stops a unit mid-flight, and nothing in the unit's own report says it happened. Whether a verifier's verdict after a compaction is worth less is unmeasured.
- B2's cost question has data on one side only after step 5 lands. `a58bd85c` gives fifteen compactions and no successor; no run gives a successor and a turn count.
- B1's 20,000-word target is an estimate built from the duplicate list and the section shares. The measured figure is what the `--corpus` lint reports after the cut.
- The Codex Sage test suite's portability defect was patched in a scratch clone for this study and not in the repository. Whether to fix it in `sage/tests/test_phase1.py` is a Codex Sage decision, outside this proposal's scope. It is one `sed` of `dir="/private/tmp"` to `dir="/tmp"`, or a `tempfile.gettempdir()` call.

## Order of work, if adopted

The order follows dependencies, not the disposition rank. Instruments land before the design they measure. Deletions land inside the rewrite that would otherwise have to move the deleted text.

1. **Scripts: A2, A5 and B2 step 5, one change set, no corpus dependency.** `sage-watch.sh` gains `model=` per actor, the evidence-ordered window and its source word, the checkpoint threshold in place of `OCC_HANDOVER_PCT=30` with the fire-once rule, and the `compact=` (parent and per unit), `turns=`, `occ-sum=` and crossing-turn fields. The threshold lands here so that the retroactive computation and every later run use one definition. `sage-lint.sh` gains `secret-shape` in both modes. Test against the 21 ledgers under `.claude/plans/` and the `a58bd85c` transcript, which must yield `claude-opus-5`, `window=measured` at 177,779 and `compact=15`. Compute B2's fields for `a58bd85c` and run the transcript check described under B2 for a re-dispatched unit or a lost finding: that is the gate's first datum, available before any corpus text changes.
2. **B2's additive half, one `/sage` run on the user's word.** The ledger template gains `### Resume state` (with `step:`, `checkpoint:` and the `sha256sum` lines) and `### Open questions and discarded approaches`. The `run` line gains the five fields. The handoff-note template is deleted from `dispatch.md`, and `/sage resume` reads the ledger. `install.sh` installs the compaction hook by default with the new message, rewrites an existing `sage-reanchor` hook in place, and mentions the `/autocompact` knob with the verify-against-docs sentence. The rung's action in today's `## Handover` becomes the checkpoint, so the sensor stops asking for a successor before the successor text is gone. `orchestrator.md` and the rest of `## Handover` stand, unused, until item 3. Depends on item 1: the rung's threshold and fields come from the script.
3. **B1, carrying B2's deletion half and the memory boundary table.** The router, the five step files, the harness split with the corrected compaction figure in the measurements file, the memory split with the boundary table as its opening and `memory-contract.md` beside `/sage-promote`, and `## Compaction and resume`. In the same run: delete `## Handover`, supervisor mode, chaining, `Generation` and `role` with the lint `header-fields` check, `SAGE_OCC_ACK`, `orchestrator.md`, every "five agent files" mention listed in B2 step 4, and the `sage-watch.sh` header's rung and ack text; add the installer's guarded removal of the installed `orchestrator.md`; give `cortex-budget` its second row and file set. One writer unit per file under a write lease, a `verifier` per file. Depends on item 2: the sections the router points at must exist.
4. **Verification and re-install.** `bin/sage-lint.sh --corpus` with the two-row budget, `/sage-promote`'s one-home grep, `references/topologies.md` #12's stop arm on any doubted paragraph, then `install.sh` and the `diff -rq` between repo and `~/.claude/skills/sage`. A run loads the installed copy; until this step runs, the next run loads the old text.

The first run whose parent compacts after item 3 is the first test of the design. Its `compact=` field, its checkpoint row and its ledger are what the gate reads. Every item above is a corpus edit to behaviour-shaping text. `references/authoring.md` governs its form, and `references/topologies.md` #12 governs its verification.
