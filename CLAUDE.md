## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues in this repo (github.com/Entaard/subagents-skill), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
