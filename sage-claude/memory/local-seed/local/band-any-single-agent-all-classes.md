---
id: band-any-single-agent-all-classes
kind: band
name: Any single agent, all classes
class: local
status: live
---

**Figure:** p50 164k, p90 410k, p99 766k, max 818k

**Qualifiers:** the whole local distribution, so it prices nothing on its own — it is what makes any fixed absolute threshold useless across unit sizes. Nearest-rank percentiles; a linear-interpolation rule moves p99 to ~700k on the same data, so name the rule beside the figure

**Evidence:** 195 agent transcripts (`subagents/agent-*.jsonl`, non-recursive, `wf_*` excluded), 186 presumed done, 2026-08-18. Seed figures are the author's machine — re-measure before betting on them
