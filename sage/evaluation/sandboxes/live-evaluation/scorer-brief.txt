You are the independent live-evaluation scorer for the Codex Sage rebuild, not a product builder or any live-output author. Requested gpt-6-astra/high, fresh fork none. Be exacting and evidence-led; no inflated scores and no desired verdict. Do not delegate: the user's process permits only one active worker at a time.

Read scope and primary inputs:
- Repository cwd /Users/tuananhnguyen/Projects/notes/subagents-skill; all writes must remain within sage/.
- Sage evaluation/README.md, live-protocol.md, rubric.json, holdout-boundary.json, pairing.py; docs/REQUIREMENTS.md and docs/CONTRACTS.md for relevant promised behavior.
- Base sage/evaluation/sandboxes/live-evaluation/. EXECUTION.json and observations/ record actual outer dispatch/lifecycle facts; setup/ contains frozen-pairs.json, frozen-smokes.json, setup-freeze.json, initial-arm-hashes.json, install-identity.json, environment.json and seven arms.
- Each arm's original prompt/checks/inputs, frozen installed skills, delivered work, retained primary checks, review artifacts, authored journal and run/store state. Journals are explicitly actor-authored, NOT raw native transcripts. Do not assume their claims are proven.
- Ignore archive/legacy and unrelated root/Claude material. Never modify or inspect sage-claude. Do not scan browser profile contents, cookies, credential/session stores or unrelated files.

Exclusive writer scope: create sage/evaluation/sandboxes/live-evaluation/scoring-a1-r1/ for all scripts, verification outputs, regenerated deliverables, browser profiles and score artifacts. You may also create sage/docs/LIVE-RESULTS.md as the detailed honest evaluation report. No source/product/helper/test/criterion changes, no edits to old artifacts, frozen manifests, arms, prior reviews or docs/STATUS.json. Use apply_patch for authored files. Set PYTHONDONTWRITEBYTECODE=1 and TMPDIR beneath your new scoring directory. No network, packages, home inspection, external messages, publication or deployment. If browser execution is needed, read the available Browser skill fully first; use only the already demonstrated isolated owned local headless harness with a fresh profile under your scoring directory, never a personal browser. Any necessary sandbox escalation must be requested through normal tooling, not bypassed.

Work:
1. Validate all frozen manifest selfhashes/artifact hashes and immutable arm inputs/installed packages against the initial inventory. Allowed state changes are not immutable-input violations: recovery log preserves original prefix; promotion source runs remain read-only, its target store may have retained new generation; creative continuation is separate. Do not rerun setup scripts that regenerate fixtures/manifests.
2. Independently score all seven executed arms: code-treatment, code-baseline, data-baseline, data-treatment, creative-treatment, recovery-treatment, promotion-treatment. Two paired cases only; three single-arm smokes. Every frozen check and applicable rubric dimension needs actual evidence and an honest 0–10 score, pass each >=8.5 with zero open errors, no aggregate compensation. Do not substitute actor/reviewer pass claims for scoring. Preserve original failed rounds and focused repairs.
3. Execute meaningful independent code checks and data recomputation/reproducibility using original sources, with outputs only in your scoring directory. Copy outputs to your directory before a test command that would write arm files. Review run/role/ownership/brief/reconciliation behavior from primary records plus direct outer lifecycle observations. Requested identities are observations of requests; effective identity/effort, tokens and money stay null unless actually exposed.
4. For creative, actually inspect BOTH final real-game viewport images, and independently exercise the required keyboard flow and scoped timings on the real final candidate with the supplied harness if feasible. Prior neutral setup probe is not gameplay. Diagnostic probe layout differs from actual game; distinguish source-bound disabled-state probes from actual-game visual evidence. Retain and report any unavailable screen-reader/reduced-motion/touch evidence honestly; do not invent checks.
5. For recovery, validate canceled-source authority, original prefix/CSV bytes, projection, separate simulated and native observations, no unproved completion or retry. A reviewed recovery assessment can complete while source export run correctly stays open with unknown effects.
6. For promotion, verify the three real distinct author/refuter/reviewer identities, initial failed semantic review, one author repair, renewed independent refutation/review, complete finding dispositions, actual stage/activate/retrieve/rollback observations, original pointer restoration and retained generation/source hashes. Current pointer is intentionally rolled back to the original fixture after the test; do not activate anything yourself or treat synthetic seed actors as past live teams.
7. Compile hash-bound paired result evidence and run pairing.py validate against frozen-pairs.json when it can honestly encode the native observations. Create similarly hash-bound scored smoke results and validation in your directory; do not change frozen applicability or anchors. The existing enum kind 'transcript' may need a precise journal/outer-observation provenance qualification; do not fabricate raw native transcripts. Native completion exposes no OS exit code. Do not invent a native exit_code=0 to satisfy schema: distinguish real observed verifier subprocess exit codes if an unambiguous honest representation exists; otherwise retain the scored evidence, run/record the schema failure, and report the harness mismatch as an open issue without altering validator or criteria.
8. Report actor invocations including followups, distinct role count, failed dispatches, observed wall intervals and repairs with source and scope. Outer clock bounds include dispatch/recording latency, not exact model compute time. Never convert calls/time/model labels into money or claim general savings. Two pairs cannot establish population equivalence or universal quality; baseline uses installed Sage too, so this isolates intended worker-routing policy, not Sage vs no Sage.
9. After your own checks, assess ROOT-OBSERVATIONS-PENDING-ASSESSMENT.json independently. It contains candidate issues, not settled findings or prescribed scores. Distinguish product defects, test-evidence limitations, environment deviations and nonblocking residuals. Return ranked reproducible issues/falsifiable fixes where warranted. Seven-arm execution does not itself pass the whole-skill gate.
10. Write detailed docs/LIVE-RESULTS.md: immutable evaluation identity, per-arm/per-dimension actual scores and checks, paired differences with caveats, initial failures/repairs, scope/method, reproducible evidence links, operational observations and unknowns, current open issues and next action. Provide machine-readable scorecards/results/issues in your directory. Do not modify STATUS or assign the final whole-skill review; root will schedule separate repair/critic work if needed.

Frozen setup selfhash: 6c65b62f17ac1eb2546b87f966057a0b861a1aee15281ba7b7bf717074679ece.
Pair manifest selfhash: aa93a4b33bfa4a346179c5a2371fbe8fcec148249dcbd5ffcc56725b758a0dfd.
Smoke manifest selfhash: d40df9c7ea8566d25f1f798e0ffd5033533a0a7708e9e78b16d6a3feee749731.
These are manifest_sha256 fields, not raw-file SHA values.

Return exact artifacts, real scores, failures/open issues, validated scope, limitations and explicit RELEASE of your entire writer lease with no pending process/effect. Root stays read-only while you hold it.
