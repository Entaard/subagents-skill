# Bounded promotion and rollback report

Completed one `correct` action for stable ID `museum-csv-field-count`, revision 2 with `prior_revision: 1`, after three actual independent actors and one focused author repair. The coordinator staged, validated, activated and retrieved the supported scoped fact, then performed the requested rollback. Final active pointer is **controlled-prior**: ordinary retrieval again returns revision 1's original comma-count rule. The reviewed revision 2 remains retained, not active.

All paths below are relative to this arm root:
`/Users/tuananhnguyen/Projects/notes/subagents-skill/sage/evaluation/sandboxes/live-evaluation/setup/arms/promotion-treatment`

## Delivered evidence and artifacts

- `work/proposal-final.json`: assembled author content plus exact current live refutation/review decisions; SHA256 `13d91f49429ad82bbbe0a888e24c1e02b372b1975f07457a2b4ed07c8a1d49e9`.
- `state/knowledge/generations/promotion-live-v2/`: retained complete revision-2 generation.
- `evidence/landing-checks.json`: exact commands, exit codes and returned output for validation, before/during/after retrieval, stage, activation and rollback.
- `evidence/final-verification-passed.json`: byte restoration, retained-history, frozen artifacts, source qualification and repeated sample checks; all 20 checks true.
- `evidence/actor-journal.md`: actor-authored journal, not raw native transcript.
- `state/runs/promotion-live/`: installed-Sage authoritative events, snapshot and derived report.
- Original candidate, initial refutation, failed review, single repaired candidate and renewed refutation/review remain separately preserved in `work/`, with role evidence reports and normalized outer lifecycle observations in `evidence/`.

## Evidence and semantic decision

Exactly two selected source runs, `state/runs/csv-old` and `state/runs/csv-new`, terminal-validate as completed, sequence 8, reconciled effects. Their source logs/snapshots and all initial knowledge bytes are unchanged. No quarantine was needed. `inputs/fixture-provenance.json` identifies source runs and seed proposer/refuter/reviewer labels as synthetic scaffolding, not previous live Sage teams.

The current Python 3.11.6 stdlib probe was independently repeated. All supplied rows have three `csv.reader` fields. The new A3 quoted-comma row has four literal-split pieces and three parsed fields, firing revision 1's stated falsifier. Old unquoted rows agree under both methods and remain counterevidence/boundary evidence; A4's equal counts establish only the observed field counts.

The supported class is `scoped_fact`: direct repeatable observations plus the declared Python 3.11.6 supplied-fixture environment. It establishes no transfer to unseen museum data, other parsers/Python versions, malformed/multiline CSV or alternate dialects, and no general parsing-validation or model-effect claim. The corrected record retains the stable ID, lineage, old source evidence, counterevidence, alternatives, falsifier and scoped support. The previous rule/falsifier remain in immutable revision 1.

## Actual actors and sequential ownership

| Role | Actual native actor | Requested model / effort | Dispatch |
| --- | --- | --- | --- |
| Coordinator and landing | /root/live_promotion_treatment | gpt-6-astra / high | Fresh native coordinator |
| Author and single repair | /root/live_promotion_author | gpt-5.6-sol / high | Fresh fork none, then native followup |
| Refuter and repaired-candidate challenge | /root/live_promotion_refuter | gpt-6-astra / high | Fresh fork none, then native followup |
| Independent reviewer and final recheck | /root/live_promotion_reviewer | gpt-5.6-sol / xhigh | Fresh fork none, then native followup |

Every role's effects were reconciled against actual artifacts and explicit RELEASE plus observed terminal lifecycle before the next writer. Outer normalized lifecycle files retain original and followup observations; they are not raw transcripts. The writer sequence was coordinator checkpoint/release → author → coordinator → refuter → coordinator → reviewer → coordinator → author repair → coordinator → renewed refuter → coordinator → renewed reviewer → coordinator landing/rollback. Coordinator is none of the three content roles.

Fresh workers received original task/acceptance, required installed instructions, relevant raw sources and frozen candidate/current findings. Supplemental author/coordinator narratives and target verdicts were excluded. Renewed checks assessed actual repaired bytes and changed scope. The initial reviewer independently rejected a candidate the refuter had passed, producing the focused repair.

Observed effective identity/model/effort, tokens and money are **null** for all actors; requested routing is not asserted as effective placement or savings. The installed snapshot displays unknown for null identity fields. The local coordinator's scoped sibling lookup returned an empty list; sibling terminal lifecycle remains established by the outer native observations and explicit releases, not by that local lookup.

Environment deviation: one nested native spawn returned `agent thread limit reached`, with no handle or worker. Sequential outer dispatch/followup after each coordinator release and terminal observation was used thereafter; no repeated unchanged nested failure and no invented IDs. State role records made after resumption are explicitly retrospective to those outer observations; live briefs/checkpoints existed before each dispatch. Finite plan: one candidate, one repair consumed, no additional repair.

## Review findings and disposition

- `rf-scope`: accepted minor limitation. Removing the mandatory environment qualifier permits discovery with absent or different environment cues. No machine environment guard remains. Retrieved rule/gate rationale/evidence summary explicitly retain the Python 3.11.6 supplied-sample boundary; discovery does not establish applicability or transfer.
- `rf-strength`: independent reviewer resolved the unsupported-extension concern; coordinator accepts the residual field-count-only evidence limitation. General validation, escaping, dialect coverage and transfer are unproved.
- `rv-retrieval-actual-cues`: initial reviewer blocker fixed by the original author's sole change, `qualifier.all.environment: ["python 3 stdlib csv"] → []`. Raw `inputs/cues.json` remained unchanged. Both renewed independent checks passed, and live post-activation retrieval confirmed revision 2 supported with intact boundary text.
- `rf-retrieval-repair`: resolved by renewed review and completed coordinator CLI retrieval. No unresolved material finding remains.

The failed initial review `work/review.json` and its failed state check remain retained, superseded for the repaired artifact by `repaired-semantic-gate` and `live-retrieval-fix`; they are not relabeled passed. No second candidate repair occurred.

## Landing, retrieval and rollback

All ten knowledge CLI commands in `evidence/landing-checks.json` exited 0. Stage and activate used `--expected-current controlled-prior`; the staged parent was `controlled-prior`. Post-stage validation showed two valid retained generations with the old pointer still active. Activation moved the pointer to `promotion-live-v2`. Rollback used target `controlled-prior` and `--expected-current promotion-live-v2`.

Unchanged actual cues: domain `museum imports`, artifact `csv`, operation `count fields`. Cue-file SHA256 `3734ff44664301de73a2d9a87707bbcb3d2789519301a7aa97cd26a9ebbdab8b`; all three retrievals share fingerprint `7b4fa620a62fdf1b50210230b7792138f6b38cb6140796ecc1898567924c4d29`.

| Phase | Active generation | Retrieved ID/revision/status | Result |
| --- | --- | --- | --- |
| Before landing | controlled-prior | museum-csv-field-count / 1 / supported | Original comma-count guidance |
| During activation | promotion-live-v2 | museum-csv-field-count / 2 / supported | Scoped csv.reader field-count correction, old counterevidence and visible sample/Python boundary |
| After requested rollback | controlled-prior | museum-csv-field-count / 1 / supported | Exactly equal to before-landing retrieval |

The original candidate would have returned no match under these cues because its environment qualifier was mandatory. The author repair corrected that predicate without changing cue bytes or semantic evidence. Rollback intentionally restores the original guidance even though A3 falsifies it; this exercise does not leave the correction active or treat restored revision 1 as semantically revalidated.

| Retained artifact | SHA256 |
| --- | --- |
| controlled-prior manifest | 2cf29eed1489c27e4e05def968dde8b41befd122f4db96b4c4bfd37c25b87b04 |
| promotion-live-v2 manifest | 264f60a52810a91d650136b58629c547bbd998d8c5899e11f232c051bb5fa6e1 |
| revision-1 record | 69980f4f353b123935dab1e78c074bf4508dadeda351e8228a4ab21d2c8cf643 |
| revision-2 record | 2bb339dec080e4988a01ac4506f460244517169e52b94002b29e06c48d71df0b |
| restored current.json bytes | 5865416e744707ae21c84917d7138511eb136674d1546c47d94a9c2686dcf78d |

All six retained generation files are byte-identical to their pre-activation hashes; original source logs/snapshots and frozen role artifacts remain unchanged. Final store validation reports current `controlled-prior`, generation_count 2, ok true. Rollback preserves revision-2 history; future changes must follow retained lineage rather than reuse revision 2 for different bytes.

## Verification limitations and remaining items

No remaining human action is required for this bounded sandbox task. Correction is intentionally inactive after requested rollback. Future activation or further knowledge changes are outside this completed task.

Only this arm was modified; neither source run, installed policy, inputs, actual user knowledge, project STATUS nor product files were edited. No network, packages, publication, external messages, hosting or deployment were used.

Coordinator verification had two local tooling corrections: an initial state note used an unsupported category and was rejected without mutation; the final verification script initially compared probe row arrays with whole observation wrapper objects, so that assertion failed. Inspection showed the supplied `rows` key; fixing the comparison made all checks pass. Initial output remains `evidence/final-verification.json`, corrected output `evidence/final-verification-passed.json`. These were coordinator evidence-format/check fixes, not additional candidate repairs. An author source-validation usage error was corrected as recorded in its evidence report. None changed source or knowledge bytes.

The helper proves structural/integrity properties and expected-pointer checks, not semantic truth or a physical writer lease. Semantic support is the repeated bounded probe plus independent actor analysis. No evaluation scores, cost totals or savings are assigned.
