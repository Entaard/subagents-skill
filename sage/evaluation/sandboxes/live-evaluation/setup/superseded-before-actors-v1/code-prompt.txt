$sage Build a small dependency-free Python 3 byte-stream decoder for our fictional gallery kiosk transport. Follow this complete wire specification (no external standard needed): each frame is ASCII decimal payload byte length, colon, exactly that many arbitrary payload bytes, then comma. Length zero is valid; leading zeroes except the single digit 0, signs, whitespace and nondigits are invalid. Frames may be concatenated and every boundary may split across feed calls. Implement decoder.py exposing FrameError(ValueError), Decoder(max_payload=1024), feed(chunk: bytes)->list[bytes], finish()->None, reset()->None. feed returns complete frames in order; empty chunks are harmless. Reject invalid type arguments with TypeError; max_payload must be a nonnegative integer excluding bool (otherwise ValueError). Reject oversize frames as soon as the length proves too large without accumulating unbounded headers. Any framing error poisons the decoder: further feed/finish calls raise FrameError until reset clears all state. finish rejects any partial frame, accepts a clean boundary, and does not close the decoder. Type errors do not poison or discard state. Payloads are bytes, including commas, colons, NUL and multibyte text. Provide meaningful stdlib tests exercising arbitrary chunk boundaries, malformed/truncated frames, limit enforcement, poisoning and recovery, plus a short usage README. Preserve inputs. Keep this to one focused implementation wave, independent review and necessary focused repairs. No dependencies, network, external services or publication.
