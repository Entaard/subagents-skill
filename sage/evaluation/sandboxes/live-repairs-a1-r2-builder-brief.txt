Resume as sole builder for live-repairs-a1 ROUND 2. Round 1 FAILED independent review; do not relabel it. Read sage/docs/reviews/live-repairs-a1-r1.json, especially sole remaining open_errors LIVE-03 and exact public-CLI reproductions. All eight real scores persisted in STATUS: 8,8.5,8,9,8.5,9,8,8.5. LIVE-01, LIVE-02, LIVE-04 and DOC-01 independently verified fixed. Preserve those repairs and all prior artifacts.

Write scope now ONLY minimal LIVE-03 source/test/protocol changes, required ARCHITECTURE/IMPLEMENTATION/DECISIONS documentation, and new sage/evaluation/sandboxes/live-repairs-a1-r2-builder/. STATUS remains root-owned/read-only; all old builder/critic/live directories, original LIVE-RESULTS, original live-protocol.md, rubric.json, frozen selections/manifests and sage-claude remain read-only. One builder, no agents/questions/network/packages/browser. Read/reuse your applicable skill instructions faithfully; all authored edits apply_patch. Root read-only until RELEASE + native completed.

Architecture first: update current status and record exact cause/boundary before code: v3 trusted truthiness for run_id and timestamp fields and dereferenced execution before object validation. Define and document minimal intended v3 types before testing; preserve v2 behavior and actual original exit-2 failure. No new adapter or schema framework and no catch-all exception blanket in lieu of validating shape.

TDD: add public-CLI regressions for all four independently retained malformed inputs (run_id=true, started_at=true, finished_at object, execution=null). Keep tests self-contained for clean checkout; separately replay exact retained cases from critic on copies or read-only inputs. Their current source/snapshot validator must first demonstrate false acceptance/traceback; retain red outputs. Then explicit v3 object and primitive guards before nested access must yield structured JSON exit 2 for each, without traceback. Keep valid evidenced null-native-exit v3 passing, native/subprocess numeric and provenance/reference negatives, earlier v2 behavior and every verified repair. Root does not prescribe timestamp parsing beyond the critic's type-contract finding: choose a small clear contract and enforce what the docs claim.

Retained exact failed validator is at sage/evaluation/sandboxes/live-repairs-a1-r1-critic/without-history/sage/evaluation/pairing.py; see retained-candidate-source.json and retained-validator-reproductions.json. Never overwrite its files. The critic passed 89 tests both normally and from a clean copied source tree without historical sandboxes/archive. Rerun complete normal gate with your new --evidence-dir and confined TMPDIR/SAGE_EVALUATION_SANDBOX/PYTHONDONTWRITEBYTECODE. Exact informed historical rechecks stay labeled informed, original trial scores stay failed and original v2 validator rejection retained. Do not rerun old output-writing scripts in place; rebase every output path to your new directory if reusing.

Return new round-2 candidate/source hashes, initial red and final green actual commands/results, relevant preservation checks, remaining limitations, exact artifacts, and explicit RELEASE with no pending process/effect. Do not self-close LIVE-03 or claim a critic pass. Keep this round narrow; promotion's broader context-loss coverage question belongs to the later whole-skill gate, not this repair.
