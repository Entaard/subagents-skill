Resume as sole Sol/xhigh builder for live-repairs-a1 ROUND 3. Read the complete independent review sage/docs/reviews/live-repairs-a1-r2.json and inventories surface-summary.json, primitive-summary.json, retained-failures.json under sage/evaluation/sandboxes/live-repairs-a1-r2-critic/. Round 2 failed actual scores 8,8.5,7.5,9,8,9,8,8.5; LIVE-03 remains one open group. Do not merely patch four more examples.

Exclusive write lease: minimal native-v3 validation, synthetic public-CLI tests, native-result-protocol-v3.md, ARCHITECTURE.md and current IMPLEMENTATION/DECISIONS notes, plus NEW sage/evaluation/sandboxes/live-repairs-a1-r3-builder/. Root STATUS and all earlier source snapshots/reviews/sandboxes are read-only. Preserve source original live-protocol.md and rubric.json, frozen manifests, original LIVE-RESULTS.md and every old failure. Never touch sage-claude or any path outside sage. No agents, questions, packages, network or browser. Authored edits use apply_patch; all runtime outputs confined to new sandbox.

Architecture first: record why narrow repairs missed shared v2-era nested accesses, truthiness tests and set(reference) coercions. Define a complete bounded native-v3 preflight of consumed containers/list elements, declared text/identifier fields, and reference arrays before common validation. Cover all 25 container/element targets, seven primitive fields and all reference branches identified by the critic, including legitimate single-character reference IDs. Keep the implementation explicit and small, not a schema framework, blanket catch, authenticity engine or calendar parser. Preserve v2 behavior with version-specific enforcement.

Test first through public CLI, self-contained synthetic fixtures without historical sandbox dependencies. Retain red then green for complete bounded matrix (including all 42 retained failures, original four and valid/null-allowed controls). Separately replay exact retained fixtures read-only into new outputs. All malformed cases must exit 2 with structured JSON, no traceback or false accept. Preserve valid native completion with null exit; valid string arrays; null unobserved effective model/usage; integer subprocess exits; existing evidence/provenance/reference negatives; all four other independently verified repairs. Scope is structural types, not timestamp syntax/order.

Run the complete normal gate with new evidence dir, confined TMPDIR/SAGE_EVALUATION_SANDBOX and PYTHONDONTWRITEBYTECODE=1. Retain all mistakes/retries honestly. Ensure runnable source tests do not depend on archives/history. Preserve failed exact round-2 source and artifacts; never run an old output-writing script in place.

Return actual counts/red-green commands, source candidate manifest/hashes, preservation checks, limitations, and explicit RELEASE with no pending process/effect. Do not self-close LIVE-03 or claim a critic/final gate pass. Promotion own checkpoint/resume coverage remains for fresh whole-skill review after this module, not this repair.
