> One conflict left standing, and it needs a one-word decision. The shared.md block Review and verify are one price is out of step with skill text — a pair became a floor under a dry-round loop — and its Recogniser no longer catches the new failure shape. Nothing can pick it up mechanically: stage zero takes Kind: defect, this row is contradiction, and → retirement needs two contradictions where it has one. By the corpus's own tiebreak in ## Append — choose by what would close the row, and closing this means editing sage's corpus — it should be defect. I did not re-Kind it: re-classifying a run's observation to make it a candidate for my own repair stage is exactly the "widen a trigger to give the pass something to write" move the skill forbids.

> Explain in detail.

⏺ Here is the detail. I'll lay out the divergence first, because everything else follows from it.

  What the two homes now say

  The rule lives in two places. They no longer agree.

  shared.md — the portable record:

  ▎ Price a review round and its fix-verification round as a pair.
  ▎ - Recogniser: a plan carrying a review row and no verify row, or a verify row priced as whatever is left over.
  ▎ - Band: established

  SKILL.md:99 — the skill text:

  ▎ Price a review round and its fix-verification round as a pair — then price the follow-up rounds the loop may still need. … Step 5's review loop runs until a round comes back dry, so the pair is a floor, not the whole figure: budget at
  ▎ least one blocker/major-only follow-up … and write in the plan that the count is a condition rather than a number.

  The 2026-08-23/24 run changed the review-round rule at four sites in skill text. A pair stopped being the figure and became a floor under a loop that runs until a round comes back dry. shared.md still says the pair is the figure. A run
  may not write shared.md, so the run that made the change could only file a watch row about it.

  Why the Recogniser is the part that actually bites

  shared.md's own header sets how the file is used:

  ▎ read every rule at Step 2 before estimating and at Step 3 before briefing, and apply the ones whose Recogniser matches what is in front of you.

  So the Recogniser is a matcher, not commentary. This one catches two shapes: a plan with a review row and no verify row, or a verify row priced as leftovers.

  The new failure shape is neither. It is a plan that carries both rows, prices them properly, and budgets nothing for the follow-up rounds. That plan passes the current Recogniser cleanly, so at Step 3 the rule does not fire on the very
  failure the skill text was rewritten to prevent.

  The rule itself is not wrong. Its scope is now narrower than skill text's, and its matcher has a blind spot.

  Why no stage could pick it up — all four routes, checked

  ┌───────────────────────┬───────────────────────────────────────┬───────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │         Route         │               Requires                │            Row 38             │                                                            Blocked because                                                            │
  ├───────────────────────┼───────────────────────────────────────┼───────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Stage zero            │ Kind: defect, Status begins watching  │ Kind: contradiction           │ Wrong Kind                                                                                                                            │
  ├───────────────────────┼───────────────────────────────────────┼───────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Stage one → shared.md │ Rule not already in shared.md         │ Already there                 │ Nothing to write                                                                                                                      │
  ├───────────────────────┼───────────────────────────────────────┼───────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Stage two → skill     │ Promoted records no skill or refused  │ refused 2026-08-19            │ Suppressed — and the cell rule says never re-test a refused cell, since every legitimate refusal would pass that test and be          │
  │ text                  │                                       │                               │ re-promoted                                                                                                                           │
  ├───────────────────────┼───────────────────────────────────────┼───────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Eviction              │ Falsifier fired, or two               │ Falsifier never fired; Count  │ One contradiction, bar is two                                                                                                         │
  │                       │ contradictions                        │ 1                             │                                                                                                                                       │
  └───────────────────────┴───────────────────────────────────────┴───────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  Its falsifier is "a fix-verification round returning empty twice on work that had accepted findings." The block's own text says the verify round has never come back empty. So that route is not merely unmet — it is pointing the wrong way
  entirely.

  That is a complete enumeration, not an impression. Every mechanical route is closed.

  The Kind question

  ## Append defines the four kinds and gives an explicit tiebreak:

  ▎ When two Kinds both fit, choose by what would close the row, not by what it is about. If closing it means editing sage's own corpus or scripts, it is a defect.

  Closing row 38 means editing shared.md's block — its Rule sentence and its Recogniser. That is a corpus edit, so by the corpus's own test the row is a defect. Its own Status cell says as much: "needs /sage-promote to re-state the block
  and its recogniser; the RULE is not wrong, its scope is now narrower than skill text's."

  There is also written precedent. Watch row 25 records an unmodelled case and prescribes the same route: "file such a case as a defect row for stage zero and leave the rule standing."

  And stage zero has exactly the permission needed. It may rewrite a standing block's Rule, Qualifier, Recogniser or Falsifier — the clause exists precisely for "a Recogniser a later run disproved."

  So the repair is well-defined and owned. Only the Kind cell keeps it out of reach.

  Why I stopped — and how strong that reasoning is

  I should be precise here, because I overstated it in the summary. The skill's written rule — "never lower a threshold, round a count up, or widen a trigger to give the pass something to write" — sits in the preflight and governs
  promotion. There is no written rule about re-classifying a Kind.

  What stopped me is the shape, not a citation:

  1. The pass would create and consume its own candidate. I would edit the cell that makes the row eligible, then act on that eligibility, in one pass, with no independent step between. The preflight prints its slate before any write for
     exactly this reason — so the pass acts on a list it did not author mid-flight.
  2. Kind is a run's judgment about its own observation. A run saw the divergence and classified it. Overriding that is a judgment call, and this pass had already spent three gate rounds demonstrating that my judgment on this corpus needs
     an independent check.
  3. The case is genuinely borderline. contradiction is defensible: Contradicts names a real shared.md rule. It is just not what closes the row.

  Leaving it costs one pass of delay. Re-Kinding it wrongly puts a rule's Recogniser under my unilateral edit on a Count-1 observation, in a pass with no route left to check me.

  What it costs while it stands

  Narrower than "runs will under-budget." Skill text carries the correct guidance, and Step 2 reads skill text. The real exposure is:

  - At Step 3, rules are applied by Recogniser match, and this one misses the new shape.
  - shared.md is the file that travels between machines. Another machine pulling this repo gets the stale rule with an established band on it.
  - If skill text were ever compressed to defer to shared.md — which the compression floor permits — the stale version would win.

  Your call

  Either change one cell — contradiction → defect on that row — and the next /sage-promote picks it up mechanically at stage zero, gate and all.

  Or tell me to do the repair now. The skill runs on your word, and your word can authorise the edit directly rather than routing it through the Kind cell. If you take that route I would put the restated Rule and Recogniser through the
  degradation gate like any other stage-zero prose edit, briefed to re-measure the claim rather than read the diff — which is what killed two of my three batches today.

✻ Crunched for 1m 23s

❯ what does "do the repair now" mean? Is that to fix the skill corpus?

⏺ Good question to press on — the vocabulary matters here, and the answer is narrower than "fix the corpus." Let me check one thing first.

  Ran 1 shell command (ctrl+o to expand)

⏺ Skill text is consistent across all four sites (SKILL.md:41, :99, :203, :327) — there is no fifth stale one. So skill text is settled, and shared.md is the only half left behind.

  No — it would not touch skill text

  The skill draws a hard vocabulary line, and the preflight defines it: "the corpus itself — sage's SKILL.md and references/." shared.md is named separately in that same sentence and is not part of it. So "repair the corpus" would be the
  wrong description of this job.

  Skill text is the half that is already correct. That is the whole shape of the problem: a run moved skill text forward and could not move shared memory with it, because a run may not write shared.md.

  The repair edits two prose fields inside one block of memory/shared.md. Nothing else.

  Exactly what would change

  Rule — now:

  ▎ Price a review round and its fix-verification round as a pair. The verify round has never come back empty, and has repeatedly cost more than the review itself: a fresh or full-mandate continuation re-reads the whole corpus…

  Rule — proposed:

  ▎ Price a review round and its fix-verification round as a pair, and treat that pair as a floor rather than the whole figure. …(existing sentence unchanged)… Where the review loop runs until a round comes back dry, budget at least one
  ▎ narrowed follow-up round beyond the pair.

  Recogniser — now:

  ▎ a plan carrying a review row and no verify row, or a verify row priced as whatever is left over.

  Recogniser — proposed:

  ▎ a plan carrying a review row and no verify row; a verify row priced as whatever is left over; or a plan that prices the pair as the whole review figure, with no follow-up round budgeted beyond it.

  That third clause is the load-bearing addition. It is the shape that currently walks past the matcher untouched.

  What would not change

  - Band stays established. The rule is not retired and nothing is evicted.
  - The Qualifier and Falsifier stay as they are — with one open question below.
  - SKILL.md and references/ are not opened.
  - No other block in shared.md is touched.
  - No count, no date, no ## Rules row moves.

  Where it physically lands

  shared.md is a symlink into the repo. There is one physical copy, so the write lands in <repo>/sage-claude/memory/shared.md and shows up in git status. There is no installed-tree copy to make and no diff -rq to prove — the preflight's
  tree check excludes memory for exactly that reason.

  Afterwards, row 38's Status flips to settled → memory/shared.md <date>, verified by reading the block's fields back off disk before the cell is written.

  One judgment I would not make alone

  The current Falsifier — "a fix-verification round returning empty twice on work that had accepted findings" — falsifies the pair claim. It says nothing about the floor claim. A rule with an unfalsifiable half is the kind of thing this
  system exists to prevent.

  The honest candidate is something like: three review loops whose first verify round came back dry, so no follow-up round was ever needed. Whether to add it, and in what words, is a real design call. I would put it to the degradation gate
  briefed to re-measure rather than read — and I would flag it to you either way rather than folding it in quietly.

  So the two routes differ only in who authorises

  Change the Kind cell (contradiction → defect) and the next /sage-promote picks it up at stage zero on its own machinery, with the gate in the loop.

  Or say the word now and I make the same edit this session, through the same write machinery and the same gate. Your word substitutes for the Kind cell as the thing that makes it eligible — which is legitimate, because this skill runs on
  your word by design.

