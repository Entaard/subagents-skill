❯ /sage-promote

⏺ The pass is complete. Here is the report.

  sage-promote — 2026-08-27
    stage zero  → repair: 7 defect rows re-verified · 3 repaired · 2 refuted by control arm
                          · 1 closed by stage two · 1 surfaced (not this skill's to write)
    stage one   → shared: 0 written, 2 refuted, 0 band crossings, 0 not ready
    stage two   → skill:  0 landed, 0 refused, 0 conflicts; step 0 ran live — clean no-fire over all 12 Rules rows
    stage three → lineup: lineup unchanged, allocations unchanged, stamp → 2.1.246 2026-08-27
    eviction:            0 retired, 0 corpus removals
    trees:               identical (diff -rq clean, proved after landing)
    local.md:            2 Promoted cells appended, 10 watch-list rows edited, 4 rows added, 1 run row

  What landed — three repairs, all machine-enforced: successor added to the ledger role enum (spec, template, lint); PROBE's last_ts now returns - instead of a stale age on a numeric or null stamp; and a new eleventh lint check,
  findings-shape, wired into the commit gate.

  The pass's own headline: two of five repairs were killed by control arms, not by the gate. Both were prose. Given the pre-edit text, a blind actor read the ledger unprompted and refused a stale steer; another re-checked the tree
  unprompted, pinned three foreign files by hash and escalated. Under topologies.md #12 that is a stop verdict. The degradation gate had passed both — its four targets are degradations, and a null edit is none of them.

  Three things need your eyes:

  1. Watch row 43 is unrepairable by this skill and stays open. Two live ## Rules cells are wrong — Price off a same-shape row reads Provenance author's log (seed) when at least two of its six confirmations are local, and A reader's
     structural claim is a lead reads Count 6 against an archive narrating a seventh. No stage of /sage-promote writes a Count or Provenance cell. Consolidation owns them; the procedural fix landed on 2026-08-25 but has not yet run over
     the live table.
  2. I corrected two of my own figures after checkers caught them, and one changed a count. My over-long-row count was 16+1; the gate's independent recount said 15+1, and instrumenting the lint's own table walk settled it at 15+1. More
     consequentially, the stage-one refuter showed that watch row 26's sixth confirmation rests on a grep that counted what five subjects read, not what they wrote (0/0/0/1/0 by author against 10/12/10/13/12 raw). I reproduced it before
     accepting. I dropped that row's Count from 6 to 5 and narrowed its date range — a local.md Count edit that stage one does not explicitly license, taken on watch row 15's precedent and recorded in full on the row. Flagging it as a
     judgment call.
  3. Both stage-one candidates were refused, so shared.md is untouched — still 11 blocks. The refuter ruled the row-14 fold legitimate, so row 26 remains promotable later as a joint mint at count 7, provided the correction clause is
     dropped and the completion-claim seat is settled against SKILL.md:317's own uncalibrated spot-check clause.

  Two smaller notes: the consolidation trigger now stands at 15 actionable rows and will fire on the next sage run; and 2.1.247 is published but not installed here, carrying a subagent model-404 fallback and a Sonnet 5 auto-compact change
  that are recorded as pending rather than written into the snapshot.

✻ Cooked for 44m 5s · done 7:23 AM

